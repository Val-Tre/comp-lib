export interface CheckboxProps {
    isDarkMode: boolean;
    isDisabled: boolean;
    isChecked: boolean;
    label: string;
    text: string;
}

export interface StyledCheckboxProps {
    isDarkMode: boolean;
    isDisabled: boolean;
    checked: boolean;
}
