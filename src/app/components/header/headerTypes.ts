export interface HeaderProps {
  isDarkMode: boolean;
  onButtonClick: () => (void);
}

export interface StyledHeaderProps {
  isDarkMode: boolean;
}

export interface StyledLogoProps {
  isDarkMode: boolean;
}
